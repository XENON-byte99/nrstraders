# Core Django
Django==5.2.5
asgiref==3.9.1
sqlparse==0.5.3

# Django extensions
django-widget-tweaks==1.5.0

# Production server
gunicorn

# Static files
whitenoise

# Database
dj-database-url
psycopg2-binary

# Configuration
python-decouple

# Image handling (for VisualRequisition model)
pillow

# Optional: AI free-form bill parsing (Quick Bill "Read with AI").
# Needs ANTHROPIC_API_KEY in .env; the feature degrades gracefully without it.
anthropic
